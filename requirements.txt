# Python 3.11+ required (tested on Python 3.11–3.14)
#
# These are the versions the analysis was developed and tested against.
# To reproduce the exact environment, install the pinned versions below.
# If a pinned wheel is unavailable for your platform, the ">=" floors in the
# comments indicate the minimum versions known to work.

pandas==3.0.2          # >=2.2
numpy==2.4.4           # >=1.26
scikit-learn==1.8.0    # >=1.4   (note: import name is "sklearn"; PyPI name is "scikit-learn")
scipy==1.17.1          # >=1.11
matplotlib==3.10.9     # >=3.8
seaborn==0.13.2        # >=0.13
shap>=0.51.0           # 0.51+ ships prebuilt wheels for Python 3.14; earlier pins compile from source
xgboost==3.2.0         # >=2.0
jupyter==1.1.1         # to run the notebook
